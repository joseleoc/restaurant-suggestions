import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputContainer: {},
    input: {},
    errorMessage: {
        height: 20,
        fontWeight: "semibold",
        paddingHorizontal: 8,
    }
});