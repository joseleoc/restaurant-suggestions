import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  formContainer: {
    padding: 16,
    gap: 16,
  },
  button: {},
  inputContainer: {},
  input: {},
  errorMessage: {
    height: 20,
    fontWeight: "semibold",
    paddingHorizontal: 8,
  },
});
